<%= render @project_component do |component| %>
  <%= component.with_body do %>
    <div class="dashboard">
      <%= render "builds/list", current_tab_name: @current_tab_name %>

      <div class="build">
        <%= render "runs/show", current_tab_name: @current_tab_name %>
      </div>
    </div>
  <% end %>
<% end %>
