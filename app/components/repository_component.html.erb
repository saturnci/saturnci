<div class="project <%= @extra_css_classes %>">
  <header class="repository-header">
    <h2><%= link_to @repository.name, repository_path(@repository) %></h2>
    <%= link_to "Settings", repository_settings_general_settings_path(@repository) %>
  </header>

  <%= body %>
</div>
