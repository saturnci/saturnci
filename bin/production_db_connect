#!/bin/bash

source .env

PGPASSWORD=$PRODUCTION_DATABASE_PASSWORD psql \
  -h $PRODUCTION_DATABASE_HOST \
  -p $PRODUCTION_DATABASE_PORT \
  -U $PRODUCTION_DATABASE_USERNAME \
  $PRODUCTION_DATABASE_NAME
