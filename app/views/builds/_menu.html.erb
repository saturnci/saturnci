<ul class="run-menu tab-menu">
  <li>
    <%= link_to "Overview",
      project_build_path(test_suite_run.project_id, test_suite_run),
      class: current_tab_name == "overview" ? "active" : "" %>
  </li>

  <% test_suite_run.runs.sorted.each do |run| %>
    <li>
      <%= link_to run.name,
        run_path(run, target_tab_name),
        class: run == @run ? "active" : "" %>
    </li>
  <% end %>
</ul>
